# 🚀 البدء السريع - 5 دقائق

## الخطوة 1: التثبيت (دقيقة واحدة)

```bash
pip install -r requirements.txt
```

## الخطوة 2: التشغيل المحلي (30 ثانية)

```bash
mkdocs serve
```

افتح: [http://127.0.0.1:8000](http://127.0.0.1:8000)

## الخطوة 3: التخصيص (دقيقتان)

افتح `mkdocs.yml` وغيّر:

```yaml
site_name: اسم ويكيك        # غيّر هذا
site_author: اسمك            # وهذا
site_url: https://...        # وهذا
repo_name: username/repo     # وهذا
repo_url: https://...        # وهذا
```

## الخطوة 4: إضافة محتوى (دقيقة واحدة)

```bash
# مشروع جديد
copy templates\project-template.md docs\01-مشاريع\مشروعي.md

# مقال جديد
copy templates\new-page.md docs\02-معرفة\مقالي.md
```

## الخطوة 5: النشر (30 ثانية)

```bash
git add .
git commit -m "✨ feat: إعداد الويكي"
git push origin main
```

سيتم النشر تلقائياً على GitHub Pages!

---

## الأوامر الأساسية

```bash
mkdocs serve      # تشغيل محلياً
mkdocs build      # بناء الموقع
mkdocs gh-deploy  # نشر يدوياً
```

## المشاكل الشائعة

### `mkdocs: command not found`
```bash
pip install mkdocs mkdocs-material
```

### الموقع لا يظهر
1. Settings > Pages
2. اختر الفرع `gh-pages`
3. انتظر 2-3 دقائق

---

**للمزيد:** راجع [دليل الإعداد الكامل](docs/setup-guide.md)
