# 🤝 دليل المساهمة

## إضافة محتوى جديد

### 1. استخدام القوالب

استخدم القوالب الجاهزة في مجلد `templates/`:

```bash
# نسخ قالب صفحة جديدة
copy templates\new-page.md 02-معرفة\اسم-الصفحة.md
```

### 2. البيانات الوصفية (Frontmatter)

كل صفحة يجب أن تحتوي على:

```yaml
---
title: عنوان الصفحة
description: وصف مختصر
date_created: 2024-01-01
date_updated: 2024-01-01
tags: [وسم1, وسم2]
category: التصنيف
status: مسودة
---
```

### 3. قواعد التنسيق

- **العناوين**: استخدم H1 للعنوان الرئيسي فقط
- **الروابط**: استخدم الروابط النسبية `[نص](../مجلد/ملف.md)`
- **الصور**: احفظها في `assets/images/` واستخدم `![وصف](../assets/images/صورة.png)`
- **الأكواد**: حدد لغة البرمجة ` ```python `

### 4. اتفاقية الـ Commits

استخدم البادئات التالية:

- `📝 docs:` - لتحديث الوثائق
- `✨ feat:` - لإضافة محتوى جديد
- `♻️ refactor:` - لإعادة هيكلة المحتوى
- `📁 chore:` - لتغييرات الهيكل
- `🔧 fix:` - لإصلاح أخطاء
- `🌐 i18n:` - للترجمة

**أمثلة:**
```
📝 docs: إضافة دليل استخدام الويكي
✨ feat: إضافة قسم المشاريع الجديدة
🔧 fix: تصحيح الروابط المعطلة
```

### 5. استراتيجية الفروع

- `main` - للنسخة المستقرة
- `develop` - لتطوير محتوى جديد
- `feature/*` - لإضافة محتوى كبير

## تحديث الفهرس

بعد إضافة محتوى جديد، قم بتحديث:
1. القائمة الجانبية في `docs/.vitepress/config.js`
2. ملف `README.md` في المجلد المعني
3. الصفحة الرئيسية إذا لزم الأمر

## المراجعة

- تأكد من عدم وجود روابط معطلة
- راجع التنسيق والإملاء
- تحقق من صحة البيانات الوصفية
